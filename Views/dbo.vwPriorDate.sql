SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO



CREATE VIEW [dbo].[vwPriorDate]
AS
		SELECT 
			DISTINCT CONVERT(DATE,[tsbb]) as [datebb]
		FROM
			[TMDBSQL].[dbo].[TICKDB_TYPE]
		ORDER BY
			[datebb] DESC
		OFFSET 1 ROWS
		FETCH NEXT 1 ROWS ONLY
GO
